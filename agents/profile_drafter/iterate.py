"""E.5 — iterate_or_finalize (STUB).

Full decision-tree implementation lands in commit `[DRAFT-E.5]`.
"""

from __future__ import annotations

from typing import Any


def iterate_or_finalize(
    union: str,
    drafts_so_far: int,
    validation_result: dict[str, Any],
) -> str:
    """Stub — see [DRAFT-E.5]."""
    del union, drafts_so_far, validation_result
    raise NotImplementedError("iterate_or_finalize — implemented in [DRAFT-E.5]")
