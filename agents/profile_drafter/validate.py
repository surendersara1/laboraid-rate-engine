"""E.4 — validate_generated (STUB).

Full subprocess-based evaluator orchestration lands in commit `[DRAFT-E.4]`.
"""

from __future__ import annotations

from typing import Any


def validate_generated(
    profile_path_candidate: str,
    extractor_path_candidate: str,
    union_dir: str,
    groundtruth_path: str,
) -> dict[str, Any]:
    """Stub — see [DRAFT-E.4]."""
    del profile_path_candidate, extractor_path_candidate, union_dir, groundtruth_path
    raise NotImplementedError("validate_generated — implemented in [DRAFT-E.4]")
